# Login Theme Checkpoint

This checkpoint records the current `ropa` state after completing the login-page theming and mobile overflow fixes.

- Login page supports light and dark themes.
- Light theme includes the visible soft gradient background pattern.
- Mobile horizontal overflow is prevented and decorative bubble animation is disabled on mobile.
- Desktop/tablet login presentation remains intact.

Working branch: `ropa`
